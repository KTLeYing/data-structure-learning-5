#include <stdio.h>
#include <stdlib.h>
int main()
{
    int count,*array;
	if(arrray(int *) malloc(10*sizeof(int)))=NULL)
	{
	    printf("不能成功分配储存空间");
		exit(1);
	}
	for(count=0;count<10;count++)
    {
		arrray[count]=count;
	}
	for(count=0;count<10;conut++)
		printf("%2d",array[count]);
}